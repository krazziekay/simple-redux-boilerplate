import React, { useState, useEffect } from 'react';

const SalesInvoices = ({}) => {
  useEffect(() => {
  }, []);

  return (
    <div>
      SalesInvoices
    </div>
  );
};

export default SalesInvoices;
