import React, { useState, useEffect } from 'react';

const EstimateQuotes = ({}) => {
  useEffect(() => {
  }, []);

  return (
    <div>
      EstimateQuotes
    </div>
  );
};

export default EstimateQuotes;
