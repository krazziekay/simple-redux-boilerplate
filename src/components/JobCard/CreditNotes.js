import React, { useState, useEffect } from 'react';

const CreditNotes = ({}) => {
  useEffect(() => {
  }, []);

  return (
    <div>
      CreditNotes
    </div>
  );
};

export default CreditNotes;
